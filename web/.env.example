VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<supabase-anon-key>
# Empty locally (Vite proxies /api to Flask). On Railway: the api service public URL, no trailing slash.
VITE_API_BASE=
